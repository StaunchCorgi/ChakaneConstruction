class ContactDetails {
    constructor(fullName, contactNumber, email, service, message) {
        this.fullName = fullName;
        this.contact = contactNumber;
        this.email = email;
        this.service = service;
        this.message = message;
    }
}

module.exports = ContactDetails;